<p id="notice"><%= notice %></p>

<p>
  <strong>Name:</strong>
  <%= @company.name %>
</p>


	<div class="well">
					<%= form_with model: [ @company, @employee ], local: true do |form| %>

						<%= form.label  "first name"%>
						<%= form.text_field :first_name %>

						<%= form.label "last name"%>
						<%= form.text_field :last_name %>

            <%= form.label "email"%>
						<%= form.text_field :email %>

            <%= form.label "area"%>
						<%= form.select :area_id, Area.pluck(:name, :id), promp: 'elige'%>


						<%= form.submit "Agregar"  %>

					<% end %>
			</div>
      <div class="well">

      </div>
        <table class="table table-hover">
        <thead>
          <tr>
            <th>nombre</th>
            <th>last name</th>
            <th>email</th>
            <th>area</th>
          </tr>
        </thead>
        <tbody>
				<% @company.employees.each do |t| %>
        <tr>
          <td><%=t.first_name%></td>
          <td><%=t.last_name%></td>
          <td><%=t.email%></td>
          <td><%=t.area%></td>
        </tr>

         <tr>

					<td><%= t.first_name %> <%= link_to 'Borrar', company_employee_path(@company, t), class: 'btn btn-danger', method: :delete %></td> -->
        </tr>


          </tbody>

				</table>


				<% end %>

	</div>
</div>

		<%= link_to 'Edit', edit_company_path(@company) %> |
		<%= link_to 'Back', companies_path %>
</div>
